# pip 依赖（由 environment.yml 的 pip: 段引用；也可在已激活的 conda 环境里单独 pip install -r）
#
# 注意：pinocchio / pink / qpsolvers / quadprog / numpy / scipy 走 conda-forge，
#       不在这里列（见 environment.yml）。这里只放 pip-only 的运行时依赖。
#
# 若坚持纯 pip（不用 conda），请另装：
#   pip install pin==4.0.0 pin-pink==4.2.0 qpsolvers==4.12.0 quadprog==0.1.13 numpy scipy
#   （macOS 上 pip 装 pinocchio 可能失败/缺依赖，强烈建议用 conda-forge）

# ---- Apple Vision Pro 数据流 ----
avp-stream==2.51

# avp-stream 的 WebRTC / 服务端依赖
aiortc==1.14.0
av==16.1.0
aioice==0.10.2
pylibsrtp==1.0.0
pyee==13.0.1
cryptography==48.0.1
pyOpenSSL==26.2.0
Flask==3.1.3
Werkzeug==3.1.8
grpcio==1.81.0
grpcio-tools==1.81.0
protobuf==6.33.6
websocket-client==1.9.0

# ---- 仿真 ----
mujoco==3.9.0
glfw==2.10.0
PyOpenGL==3.1.10

# ---- Pink 运行时 ----
loop-rate-limiters==1.2.0

# ---- 常用工具（按需）----
matplotlib==3.10.9
opencv-python==4.13.0.92
gdown==6.1.0
requests==2.34.2
PyYAML==6.0.3
tqdm==4.68.2

# ---- EgoPoser 神经先验（可选，仅 --egoposer 时需要）----
# 只在启用 EgoPoser 躯干先验时才需要 torch；默认遥操作路径不导入 torch。
# CPU 版即可（网络只有 3 层 transformer，单次推理 ~3 ms，60Hz 绰绰有余）。
# 权重来自官方 Google Drive（见 avp_teleop_upper_body/egoposer 与 README），
# gdown 已在上面列出，可用 EgoPoserEstimator.download_weights() 下载。
torch>=2.2,<2.6
